version: '3.9'

services:
    # Mongo db service
    mongo:
        image: mongo
        restart: always
        ports:
            # 2717 is the port for accessing mongodb compass
            - 2717:27017
        volumes:
            - db:/data/db

    # Nginx service
    nginx:
        image: nginx:stable-alpine
        depends_on:
            - api
        volumes:
            - ./server/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
        ports:
            - 80:80

    #API service
    api:
        container_name: project-assesment-platform-api
        scale: 2
        depends_on:
            - mongo
        build: .
        volumes:
            - .:/app
            - /app/node_modules
        ports:
            - 5000:5000
        environment:
            - DOCKER_RUNNING=true
            - NODE_ENV=development
            - MONGO_URI=mongodb://mongo:27017/mern-template
            - PORT=5000
        command: yarn start

volumes:
    db:
